# Optional: Google Sheets sync.
# Deploy apps-script/Code.gs as a Google Apps Script web app and paste the /exec URL here.
VITE_SHEETS_WEB_APP_URL=""

# Optional: weak shared token checked by Apps Script.
# This is visible in the browser bundle, so do not treat it like a real password.
VITE_SHEETS_API_TOKEN=""
